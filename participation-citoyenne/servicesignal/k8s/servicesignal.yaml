apiVersion: apps/v1
kind: Deployment
metadata:
  name: servicesignal
  namespace: ${NAMESPACE}
  labels:
    app: servicesignal
spec:
  replicas: 1
  selector:
    matchLabels:
      app: servicesignal
  template:
    metadata:
      labels:
        app: servicesignal
    spec:
      containers:
        - name: servicesignal
          image: ${FULL_IMAGE_NAME}
          imagePullPolicy: Always
          ports:
            - containerPort: 5002
              name: http
          envFrom:
            - secretRef:
                name: servicesignal-secret
          resources:
            requests:
              cpu: "200m"
              memory: "256Mi"
            limits:
              cpu: "1000m"
              memory: "1Gi"
          livenessProbe:
            httpGet:
              path: /servicesignal
              port: http
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /servicesignal
              port: http
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3
          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
      imagePullSecrets:
        - name: harbor-registry-secret