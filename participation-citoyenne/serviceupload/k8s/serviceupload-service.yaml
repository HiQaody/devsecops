apiVersion: v1
kind: Service
metadata:
  name: serviceupload-service
  namespace: ${NAMESPACE}
spec:
  type: NodePort
  selector:
    app: serviceupload
  ports:
    - protocol: TCP
      port: 5012
      targetPort: 5012
      nodePort: 30512