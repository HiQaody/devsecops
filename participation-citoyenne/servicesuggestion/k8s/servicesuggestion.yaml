apiVersion: apps/v1
kind: Deployment
metadata:
  name: servicesuggestion
  namespace: ${NAMESPACE}
  labels:
    app: servicesuggestion
spec:
  replicas: 1
  selector:
    matchLabels:
      app: servicesuggestion
  template:
    metadata:
      labels:
        app: servicesuggestion
    spec:
      containers:
        - name: servicesuggestion
          image: ${FULL_IMAGE_NAME}
          imagePullPolicy: Always
          ports:
            - containerPort: 5009
              name: http
          envFrom:
            - secretRef:
                name: servicesuggestion-secret
          resources:
            requests:
              cpu: "200m"
              memory: "256Mi"
            limits:
              cpu: "1000m"
              memory: "1Gi"
          livenessProbe:
            httpGet:
              path: /servicesuggestion
              port: http
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /servicesuggestion
              port: http
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3
          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
      imagePullSecrets:
        - name: harbor-registry-secret