apiVersion: apps/v1
kind: Deployment
metadata:
  name: diamadio-admin
  namespace: pnud-agvm
spec:
  replicas: 1
  selector:
    matchLabels:
      app: diamadio-admin
  template:
    metadata:
      labels:
        app: diamadio-admin
    spec:
      containers:
      - name: diamadio-admin
        image: ${FULL_IMAGE_NAME}
        imagePullPolicy: Always
        ports:
          - containerPort: 4002
            name: http
        envFrom:
          - secretRef:
              name: diamadio-admin-secret
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "500m"
            memory: "512Mi"
        livenessProbe:
          httpGet:
            path: /
            port: http
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /
            port: http
          initialDelaySeconds: 5
          periodSeconds: 5
      imagePullSecrets:
      - name: harbor-registry-secret