apiVersion: apps/v1
kind: Deployment
metadata:
  name: serviceaffiliation
  namespace: ${NAMESPACE}
  labels:
    app: serviceaffiliation
spec:
  replicas: 1
  selector:
    matchLabels:
      app: serviceaffiliation
  template:
    metadata:
      labels:
        app: serviceaffiliation
    spec:
      containers:
        - name: serviceaffiliation
          image: ${FULL_IMAGE_NAME}
          imagePullPolicy: Always
          ports:
            - containerPort: 5036
              name: http
          envFrom:
            - secretRef:
                name: serviceaffiliation-secret
          resources:
            requests:
              cpu: "200m"
              memory: "256Mi"
            limits:
              cpu: "1000m"
              memory: "1Gi"
          livenessProbe:
            httpGet:
              path: /serviceaffiliation
              port: http
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /serviceaffiliation
              port: http
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3
          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
      imagePullSecrets:
        - name: harbor-registry-secret