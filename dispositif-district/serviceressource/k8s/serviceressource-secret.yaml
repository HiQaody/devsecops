apiVersion: v1
kind: Secret
metadata:
  name: serviceressource-secret
  namespace: ${NAMESPACE}
type: Opaque
stringData:
  DB_HOST: "${DB_HOST}"
  DB_PORT: "${DB_PORT}"
  DB_USER: "${DB_USER}"
  DB_PASS: "${DB_PASS}"
  BASE_URL: "${BASE_URL}"
  DB_NAME: "${DB_NAME}"