apiVersion: apps/v1
kind: Deployment
metadata:
  name: serviceressource
  namespace: ${NAMESPACE}
  labels:
    app: serviceressource
spec:
  replicas: 1
  selector:
    matchLabels:
      app: serviceressource
  template:
    metadata:
      labels:
        app: serviceressource
    spec:
      containers:
        - name: serviceressource
          image: ${FULL_IMAGE_NAME}
          imagePullPolicy: Always
          ports:
            - containerPort: 5031
              name: http
          envFrom:
            - secretRef:
                name: serviceressource-secret
          resources:
            requests:
              cpu: "200m"
              memory: "256Mi"
            limits:
              cpu: "1000m"
              memory: "1Gi"
          livenessProbe:
            httpGet:
              path: /serviceressource
              port: http
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /serviceressource
              port: http
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3
          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
      imagePullSecrets:
        - name: harbor-registry-secret