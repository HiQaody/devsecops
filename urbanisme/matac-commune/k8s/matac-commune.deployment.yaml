apiVersion: apps/v1
kind: Deployment
metadata:
  name: matac-commune
  namespace: pnud-agvm
spec:
  replicas: 1
  selector:
    matchLabels:
      app: matac-commune
  template:
    metadata:
      labels:
        app: matac-commune
    spec:
      containers:
      - name: matac-commune
        image: ${FULL_IMAGE_NAME}
        imagePullPolicy: Always
        ports:
        - containerPort: 4016
          name: http
        env:
        - name: PORT
          value: "4016"
        envFrom:
        - secretRef:
            name: matac-commune-secret
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "500m"
            memory: "512Mi"
        livenessProbe:
          httpGet:
            path: /
            port: 4016
          initialDelaySeconds: 30
          periodSeconds: 10
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /
            port: 4016
          initialDelaySeconds: 5
          periodSeconds: 5
          failureThreshold: 3
      imagePullSecrets:
      - name: harbor-registry-secret