apiVersion: apps/v1
kind: Deployment
metadata:
  name: servicecarto
  namespace: ${NAMESPACE}
  labels:
    app: servicecarto
spec:
  replicas: 1
  selector:
    matchLabels:
      app: servicecarto
  template:
    metadata:
      labels:
        app: servicecarto
    spec:
      containers:
        - name: servicecarto
          image: ${FULL_IMAGE_NAME}
          imagePullPolicy: Always
          ports:
            - containerPort: 5040
              name: http
          envFrom:
            - secretRef:
                name: servicecarto-secret
          resources:
            requests:
              cpu: "200m"
              memory: "256Mi"
            limits:
              cpu: "1000m"
              memory: "1Gi"
          livenessProbe:
            httpGet:
              path: /servicecarto
              port: http
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /servicecarto
              port: http
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3
          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
      imagePullSecrets:
        - name: harbor-registry-secret