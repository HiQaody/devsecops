apiVersion: apps/v1
kind: Deployment
metadata:
  name: anjaranaka
  namespace: pnud-agvm
spec:
  replicas: 1
  selector:
    matchLabels:
      app: anjaranaka
  template:
    metadata:
      labels:
        app: anjaranaka
    spec:
      containers:
      - name: anjaranaka
        image: ${FULL_IMAGE_NAME}
        imagePullPolicy: Always
        ports:
        - containerPort: 4005
          name: http
        env:
        - name: PORT
          value: "4005"
        envFrom:
        - secretRef:
            name: anjaranaka-secret
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "500m"
            memory: "512Mi"
        livenessProbe:
          httpGet:
            path: /
            port: 4005            # ou "http" si vous préférez le nom du port
          initialDelaySeconds: 30
          periodSeconds: 10
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /
            port: 4005            # ou "http"
          initialDelaySeconds: 5
          periodSeconds: 5
          failureThreshold: 3
      imagePullSecrets:
      - name: harbor-registry-secret