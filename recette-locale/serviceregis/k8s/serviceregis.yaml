apiVersion: apps/v1
kind: Deployment
metadata:
  name: serviceregis
  namespace: ${NAMESPACE}
  labels:
    app: serviceregis
spec:
  replicas: 1
  selector:
    matchLabels:
      app: serviceregis
  template:
    metadata:
      labels:
        app: serviceregis
    spec:
      containers:
        - name: serviceregis
          image: ${FULL_IMAGE_NAME}
          imagePullPolicy: Always
          ports:
            - containerPort: 5027
              name: http
          envFrom:
            - secretRef:
                name: serviceregis-secret
          resources:
            requests:
              cpu: "200m"
              memory: "256Mi"
            limits:
              cpu: "1000m"
              memory: "1Gi"
          livenessProbe:
            httpGet:
              path: /serviceregis
              port: http
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /serviceregis
              port: http
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3
          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
      imagePullSecrets:
        - name: harbor-registry-secret