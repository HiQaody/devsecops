apiVersion: apps/v1
kind: Deployment
metadata:
  name: servicebien
  namespace: ${NAMESPACE}
  labels:
    app: servicebien
spec:
  replicas: 1
  selector:
    matchLabels:
      app: servicebien
  template:
    metadata:
      labels:
        app: servicebien
    spec:
      containers:
        - name: servicebien
          image: ${FULL_IMAGE_NAME}
          imagePullPolicy: Always
          ports:
            - containerPort: 5029
              name: http
          envFrom:
            - secretRef:
                name: servicebien-secret
          resources:
            requests:
              cpu: "200m"
              memory: "256Mi"
            limits:
              cpu: "1000m"
              memory: "1Gi"
          livenessProbe:
            httpGet:
              path: /servicebien
              port: http
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /servicebien
              port: http
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3
          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
      imagePullSecrets:
        - name: harbor-registry-secret