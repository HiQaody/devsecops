apiVersion: v1
kind: Secret
metadata:
  name: criv-client-secret
  namespace: ${NAMESPACE}
type: Opaque
stringData:
  VITE_API_URL: "${VITE_API_URL}"
  VITE_PORT: "${VITE_PORT}"
  VITE_FEEDBACK_URL: "${VITE_FEEDBACK_URL}"