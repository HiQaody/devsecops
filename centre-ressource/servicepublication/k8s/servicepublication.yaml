apiVersion: apps/v1
kind: Deployment
metadata:
  name: servicepublication
  namespace: ${NAMESPACE}
  labels:
    app: servicepublication
spec:
  replicas: 1
  selector:
    matchLabels:
      app: servicepublication
  template:
    metadata:
      labels:
        app: servicepublication
    spec:
      containers:
        - name: servicepublication
          image: ${FULL_IMAGE_NAME}
          imagePullPolicy: Always
          ports:
            - containerPort: 5011
              name: http
          envFrom:
            - secretRef:
                name: servicepublication-secret
          resources:
            requests:
              cpu: "200m"
              memory: "256Mi"
            limits:
              cpu: "1000m"
              memory: "1Gi"
          livenessProbe:
            httpGet:
              path: /servicepublication
              port: http
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /servicepublication
              port: http
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3
          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
      imagePullSecrets:
        - name: harbor-registry-secret