apiVersion: apps/v1
kind: Deployment
metadata:
  name: serviceentreprise
  namespace: ${NAMESPACE}
  labels:
    app: serviceentreprise
spec:
  replicas: 1
  selector:
    matchLabels:
      app: serviceentreprise
  template:
    metadata:
      labels:
        app: serviceentreprise
    spec:
      containers:
        - name: serviceentreprise
          image: ${FULL_IMAGE_NAME}
          imagePullPolicy: Always
          ports:
            - containerPort: 5003
              name: http
          envFrom:
            - secretRef:
                name: serviceentreprise-secret
          resources:
            requests:
              cpu: "200m"
              memory: "256Mi"
            limits:
              cpu: "1000m"
              memory: "1Gi"
          livenessProbe:
            httpGet:
              path: /serviceentreprise
              port: http
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /serviceentreprise
              port: http
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3
          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
      imagePullSecrets:
        - name: harbor-registry-secret