apiVersion: v1
kind: Secret
metadata:
  name: serviceterritoire-v2-secret
  namespace: ${NAMESPACE}
type: Opaque
stringData:
  POSTGRES_HOST: "${POSTGRES_HOST}"
  POSTGRES_PORT: "${POSTGRES_PORT}"
  POSTGRES_USER: "${POSTGRES_USER}"
  POSTGRES_PASSWORD: "${POSTGRES_PASSWORD}"
  IMAGE_URL: "${IMAGE_URL}"
  POSTGRES_DB: "${POSTGRES_DB}"