apiVersion: apps/v1
kind: Deployment
metadata:
  name: serviceflotte
  namespace: ${NAMESPACE}
  labels:
    app: serviceflotte
spec:
  replicas: 1
  selector:
    matchLabels:
      app: serviceflotte
  template:
    metadata:
      labels:
        app: serviceflotte
    spec:
      containers:
        - name: serviceflotte
          image: ${FULL_IMAGE_NAME}
          imagePullPolicy: Always
          ports:
            - containerPort: 5021
              name: http
          envFrom:
            - secretRef:
                name: serviceflotte-secret
          resources:
            requests:
              cpu: "200m"
              memory: "256Mi"
            limits:
              cpu: "1000m"
              memory: "1Gi"
          livenessProbe:
            httpGet:
              path: /serviceflotte/docs
              port: http
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /serviceflotte/docs
              port: http
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3
          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
      imagePullSecrets:
        - name: harbor-registry-secret